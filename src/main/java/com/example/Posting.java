package com.example;

public class Posting {
    public void methods() {
        // posting
        // Laptop l1=new Laptop((long) 101, "Macbook");
        // Student s1=new Student((long) 1, "vans", 32, l1);
        // Student s2=new Student();
        // s2.setId((long)32);
        // s2.setName("vans");
        // s2.setMarks(32);
        // Laptop l1=new Laptop();
        // l1.setId((long)101);
        // l1.setName("dell");
        // Laptop l2=new Laptop();
        // l2.setId((long)102);
        // l2.setName("mac");
        // List<Laptop> arrL=new ArrayList<Laptop>();
        // arrL.add(l1);
        // arrL.add(l2);
        // s2.setLaptops(arrL);
        // l1.setStudent(s2);
        // l2.setStudent(s2);
        // session.save(l1);
        // session.save(l2);
        // session.save(s2);

        // session.save(s1);
        // getting

        // get vs load
        // load give a blank proxy object, will not hit database unless it is used

        // load doesn't give null but just throws object not found exception when not
        // found in database
    }
}
